{% extends 'layout.html.twig' %}

{% block body %}

<h1>Users Management</h1>

{{form_start(search)}}

    {{form_row(search.title)}}

    {{form_widget(search.search)}}

{{form_end(search)}}

<ul>
{% for user in users %}
    <li id="user{{user.id}}" data-userId="{{user.id}}">{{user.username}}, <a href="#">modifier</a>, <a href="#">supprimer</a></li>
{% endfor %}
</ul>

{% endblock %}